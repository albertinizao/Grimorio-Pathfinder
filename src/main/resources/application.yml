spring:
  application:
    name: grimorio-pathfinder
  profiles:
    default: local
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
  sql:
    init:
      mode: never

server:
  port: 8081
